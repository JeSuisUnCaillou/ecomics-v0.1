
<%= form_for @frame, :url => frames_path, :html => { :multipart => true } do |f| %>
  <div class="field">
    <%= f.file_field :picture %>
  </div>
  <!--div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div-->
  <!--div class="field">
    <%= f.label :duration %>
    <%= f.number_field :duration %> ms
  </div-->
  <div class="field">
    <%= f.hidden_field :ecomic_id, value: ecomic.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>

